// DynamicDeclaration.cpp : Program to demonstrate dynamic declaration in C++

#include <iostream>
using namespace std;

int main()
{
    int a = 4, b = 3;
	if (int i=4;i==a)
	{
		cout << "i is dynamically declared. It is only used when if statement is called.";
	}
}